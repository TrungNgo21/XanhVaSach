<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when checked -->
    <item android:drawable="@drawable/input_field" android:state_checked="false" />
    <!-- Default state (unchecked) -->
    <item android:drawable="@drawable/success_chip" android:state_checked="true" />
</selector>